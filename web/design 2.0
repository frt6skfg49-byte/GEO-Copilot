# Digital Culture 官网设计规范

## 一、核心设计原则
1. **极简克制**：以内容为核心载体，减少冗余装饰元素，通过大面积留白与严谨排版营造高端专业的商务质感
2. **动态叙事**：以滚动触发为核心动效逻辑，用动画串联全页内容，传递品牌"数字化演进、系统成长"的核心概念
3. **层级清晰**：严格的排版与区块划分体系，保证信息可读性与逻辑递进性，引导用户流畅浏览全页
4. **轻量化交互**：微交互克制不突兀，仅作为操作反馈与视觉引导，不干扰核心内容的浏览体验

## 二、UI 页面设计规范
### 2.1 整体布局
- **布局模式**：单页垂直滚动布局（Single Page Scroll），全页按业务模块纵向划分，无复杂多栏嵌套结构
- **栅格系统**：宽屏居中栅格架构，内容区约束最大宽度，左右保留大量留白，优先适配桌面端大屏浏览
- **区块逻辑**：每个业务模块独立成区，区块之间通过留白+装饰性元素分隔，区隔清晰无拥挤感
- **页面结构顺序**：
  1. 英雄首屏区（Hero Section）
  2. 品牌定位与价值主张区
  3. 核心能力服务区（6大能力模块）
  4. 覆盖行业展示区
  5. 公司数据概览区
  6. 客户评价与合作伙伴区
  7. 行业洞察/内容博客区
  8. 常见问题FAQ区
  9. 底部行动召唤（CTA）区

### 2.2 排版体系
- **字体选型**：采用现代无衬线字体（Sans-serif），笔画均匀简洁，支持多字重层级，适配科技商务的品牌调性
- **标题层级**：
  1. **一级标题**（板块主标题）：超大字号、粗字重，作为每个区块的视觉锚点，如 `Our capabilities`、`Industries we empower`
  2. **二级标题**（子模块标题）：中大号字号、中等字重，用于每个能力模块的主题，如 `Brand Intelligence`、`Product Elevation`
  3. **三级标题**（条目小标题）：常规字号，用于服务条目、FAQ问题、文章标题等
  4. **正文文本**：小号字号、常规字重，用于描述性文本、服务说明、问答答案等
- **特色排版设计**：
  - 英雄首屏采用**重复式品牌名排版**：多行重复 `Digital Culture` 文字作为首屏视觉主体，强化品牌记忆点
  - 数据概览区采用「大数字+小字说明」的排版形式，突出核心业绩指标
  - 客户评价区采用「引文内容+客户署名+所属品牌」的经典 testimonial 排版，正式且有信任感

### 2.3 色彩体系
- **基底色**：以白色/极浅灰为页面主背景，深灰/纯黑为正文文字色，保证高对比度与强可读性
- **强调色**：采用低饱和度科技色系（如深空灰、藏蓝色），仅用于重点按钮、交互元素、强调文字，克制不张扬
- **中性色**：多层级灰色系，用于次要文字、分隔线、背景块，清晰区分信息层级
- **整体色彩倾向**：以黑白灰为色彩主体，低饱和度、少色彩变化，营造专业、冷静、高端的咨询品牌气质

### 2.4 核心组件规范
1. **顶部导航组件**：固定顶部导航栏，极简样式仅保留核心导航项；滚动时可伴随背景透明度变化，保证导航可读性
2. **服务模块卡片**：每个能力模块为独立区块，采用「标题+描述+服务条目列表」的结构，无多余装饰，依靠排版区分层级
3. **数据指标组件**：横向均匀排列4组数据，每组为「数值+指标名称」，居中对齐，视觉均衡
4. **客户评价组件**：引文内容+客户姓名+所属公司+品牌logo，简约正式，强化品牌信任背书
5. **FAQ折叠面板**：问答式折叠组件，点击问题展开答案，搭配展开/收起状态标识，支持平滑过渡
6. **CTA按钮**：极简样式，文字搭配可选箭头标识，悬停有明确状态变化，引导用户行动

### 2.5 视觉装饰元素
- **极简几何装饰**：页面中重复出现标识性几何装饰元素，为极简线条/点状图形，分布在区块之间，作为视觉分隔与节奏调节
- **留白设计**：大量运用正负空间，通过留白营造呼吸感，提升页面高级质感，避免信息拥挤
- **低图片依赖**：以文字和排版为主要视觉载体，极少使用大尺寸图片，符合B端咨询的专业属性

## 三、动画设计规范
### 3.1 全局动画原则
- **触发逻辑**：以滚动触发为核心，所有区块动画均在元素进入视口时触发，符合用户浏览的叙事逻辑
- **动效节奏**：采用平滑缓动曲线（ease-out），动效速度偏慢，营造沉稳高级的质感，避免快节奏闪动
- **错落时序**：同一区块内的不同元素，按先后顺序错峰触发动画，形成自然的视觉引导，避免整体同时出现的生硬感

### 3.2 分区块动画设计
1. **首屏英雄区动画**
   - 入场动画：页面加载后，首屏重复的品牌文字逐行/逐字淡入，主slogan `Experience that moves.` 随后渐显，品牌定位文案最后出现，形成由主到次的入场顺序
   - 视差效果：背景层的重复品牌文字与前景内容滚动速度不同，形成纵向视差，增加页面空间深度

2. **内容区块入场动画**
   - 通用效果：每个板块的标题先执行从下往上淡入（Fade In Up），延迟后板块内容（文字、列表、卡片）依次同方向淡入，位移距离小，动效轻盈克制
   - 列表类内容：行业列表、服务列表采用错落式渐入，每个条目延迟数十毫秒依次出现，形成流畅的序列感

3. **数据概览区动画**
   - 数字计数动效：当数据区块进入视口时，4个核心数值从0开始，以缓动曲线计数增长到目标数值（120+、4B、95%、200+），同步伴随文字淡入，强化数据冲击力
   - 数值增长节奏：先快后慢，结束时有轻微回弹效果，提升动效精致度

4. **客户评价区动画**
   - 评价内容从单侧滑入+淡入，品牌logo同步渐显，整体动效舒缓沉稳，符合信任背书的内容属性
   - 若为轮播形式，切换时采用淡入淡出的平滑过渡，无生硬跳转

5. **分隔装饰元素动画**
   - 页面中重复的装饰性几何元素，随滚动呈现渐变、微位移等动态效果，作为页面节奏的视觉点缀，不抢夺内容注意力

### 3.3 微交互动画
1. **按钮交互**
   - 悬停状态：按钮文字颜色过渡到强调色，或底部下划线延伸，或轻微向右位移并显现箭头，反馈轻盈明确
   - 点击状态：轻微缩放反馈，确认用户点击动作

2. **卡片/条目交互**
   - 悬停状态：卡片轻微向上位移，同时阴影柔和加深，暗示可交互属性；文字条目可伴随文字颜色的轻微变化

3. **FAQ折叠面板**
   - 展开/收起：执行高度平滑过渡，展开时答案内容从上到下渐显，收起箭头同步旋转过渡，动效流畅无卡顿

4. **文字链接交互**
   - 悬停时文字颜色平滑过渡到强调色，或下划线从无到有延伸，交互反馈清晰

### 3.4 滚动体验优化
- **全局平滑滚动**：页面锚点跳转、鼠标滚轮滚动均采用平滑过渡，避免生硬跳动
- **性能优化**：所有动画采用CSS3/GPU加速实现，保证滚动全程流畅无掉帧
- **导航滚动态**：顶部导航随页面滚动，可实现背景从透明到实色的渐变过渡，保证导航文字可读性

## 四、适配与降级原则
- **桌面端优先**：核心视觉与动效针对桌面大屏优化，保证宽屏下的视觉冲击力与浏览体验
- **响应式适配**：移动端下简化动效，调整排版为单列布局，保证内容可读性；弱化非必要动画以提升页面性能
- **降级策略**：对低性能设备，自动弱化/关闭复杂动画，保证基础浏览体验不受影响